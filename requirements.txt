
mcp>=1.2.0,<2.0
qdrant-client>=1.12,<1.14
httpx>=0.27.0
PyYAML>=6.0
# PDF ingestion support (ingest.py) — text extraction for .pdf knowledge docs.
pypdf>=4.0

# Local BM25 sparse vectors for hybrid search (retrieval step 1). Runs offline,
# no API key. Optional at runtime: if it fails to import, rag-mcp falls back to
# dense-only (see vectorstore.py). Pulls onnxruntime.
fastembed>=0.3,<0.8
